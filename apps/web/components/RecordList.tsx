export function RecordList({title,rows}:{title:string;rows:any[]}){
	return <section className='card'><h2>{title}</h2>{rows?.length?rows.map(r=><article className='record-item' key={r.id || `${title}-${JSON.stringify(r)}`}><div className='record-head'><h3>{r.name || r.title || r.code || 'Record'}</h3><span className='record-id'>{r.id || 'No ID'}</span></div><dl className='record-meta'>{Object.entries(r).filter(([k])=>!['id','name','title'].includes(k)).slice(0,5).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{typeof v === 'object' ? JSON.stringify(v) : String(v)}</dd></div>)}</dl><details><summary className='drop-summary'><span>Full payload</span><span className='drop-arrow'>▾</span></summary><pre>{JSON.stringify(r,null,2)}</pre></details></article>):<p>No records yet.</p>}</section>
}
